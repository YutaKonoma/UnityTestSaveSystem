using UnityEngine;

public class DataManager : MonoBehaviour
{
    void Start()
    {
        ClearStageData data = new();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
